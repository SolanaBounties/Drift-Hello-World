[package]
name = "drift-hello-world"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "drift_hello_world"

[features]
default = []

[dependencies]
# TODO: Add anchor-lang and drift dependencies here
