# Drift Hello World - $200 Bounty

1. **Rust Program** (`programs/drift-hello-world/src/lib.rs`)
   - Add Drift imports
   - Create function to interact with Drift
   - Handle user accounts

2. **Frontend** (`packages/web/pages/index.tsx`) 
   - Connect wallet
   - Connect to Drift
   - Show user balance
   - Add basic trading

3. **Setup** (`Anchor.toml`, `Cargo.toml`, `package.json`)
   - Add missing dependencies
   - Configure program ID

Start do this

npm install
cd packages/web && npm install
npm run web

# Drift-Hello-World
