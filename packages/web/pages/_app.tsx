// TODO: Import wallet adapter providers
// TODO: Set up Solana connection (devnet)
// TODO: Configure wallet adapters (Phantom, Solflare, etc.)

export default function App({ Component, pageProps }) {
  // TODO: Set up wallet providers here
  
  return <Component {...pageProps} />
}
